import React from "react";
import { Lock, CheckCircle2 } from "lucide-react";
interface BadgeProps { children?: React.ReactNode; variant?: "locked" | "verified" | "neutral"; className?: string; }
export function Badge({ children, variant = "neutral", className = "" }: BadgeProps) { const styles = variant === "verified" ? "border-blue-200 bg-blue-50 text-primary" : variant === "locked" ? "border-border bg-muted text-muted-foreground" : "border-border bg-background text-muted-foreground"; return <span className={`inline-flex items-center gap-1.5 rounded-full border px-2.5 py-1 text-xs font-medium ${styles} ${className}`}>{variant === "locked" && <Lock aria-hidden="true" />}{variant === "verified" && <CheckCircle2 aria-hidden="true" />}{children || (variant === "locked" ? "Verification Pending" : variant === "verified" ? "Skill Verified" : "Status")}</span>; }
